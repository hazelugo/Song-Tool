---
phase: 04-playlist-builder
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/playlists/route.ts
  - src/app/playlists/page.tsx
  - src/components/playlist-builder.tsx
autonomous: true
gap_closure: true
requirements: [PLAY-01, PLAY-02]
must_haves:
  truths:
    - "Playlists list shows song count for each playlist"
    - "Playlists list date is prefixed with 'Last updated:'"
    - "Saving a playlist in PlaylistBuilder navigates to the new playlist detail page"
  artifacts:
    - path: "src/app/api/playlists/route.ts"
      provides: "Song count aggregate via LEFT JOIN"
      contains: "playlistSongs"
    - path: "src/app/playlists/page.tsx"
      provides: "Song count display and labeled date in playlist rows; correct redirect after save"
      contains: "songCount"
  key_links:
    - from: "src/app/api/playlists/route.ts"
      to: "playlistSongs table"
      via: "LEFT JOIN + count aggregate"
      pattern: "songCount"
    - from: "src/app/playlists/page.tsx"
      to: "handleSave"
      via: "setShowBuilder(false) before router.push"
      pattern: "setShowBuilder.*false"
---

<objective>
Fix the playlists list page to show song counts and labeled dates, and fix the save redirect so PlaylistBuilder navigates to the new playlist's detail page.

Purpose: Three UAT failures stem from the playlists list page and save flow — song count missing, date unlabeled, and wrong redirect after save.
Output: Working playlists list with song counts and labeled dates; save correctly navigates to /playlists/[id].
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/api/playlists/route.ts
@src/app/playlists/page.tsx
@src/components/playlist-builder.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add song count to playlists API and fix list display</name>
  <files>src/app/api/playlists/route.ts, src/app/playlists/page.tsx</files>
  <action>
In `src/app/api/playlists/route.ts` GET handler:
- Replace the simple `db.select().from(playlists)` query with a LEFT JOIN to `playlistSongs` that computes a song count per playlist.
- Use Drizzle's `sql` template or a subquery approach. Specifically:
  - Select `playlists.id`, `playlists.name`, `playlists.updatedAt`, and a count of `playlistSongs` rows as `songCount`.
  - Use a LEFT JOIN on `playlistSongs.playlistId = playlists.id`.
  - Group by `playlists.id`.
  - Keep existing WHERE (userId match, deletedAt is null), ORDER BY (desc updatedAt), LIMIT/OFFSET.
- The response shape changes: each item now includes `songCount: number`.

In `src/app/playlists/page.tsx`:
- Update the `PlaylistSummary` interface to add `songCount: number`.
- In the playlist row markup, after the playlist name span, add a song count indicator: `<span className="text-xs font-mono text-muted-foreground tabular-nums">{playlist.songCount} song{playlist.songCount !== 1 ? 's' : ''}</span>`.
- Change the date display from raw `toLocaleDateString()` to include a label: prefix with "Last updated " — e.g., `Last updated 3/10/2026`.
- Keep the existing layout structure (Link with flex, name on left, metadata on right). Place song count between name and date.
  </action>
  <verify>
    <automated>cd /Users/hector/Developer/Song\ Tool && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Playlists list API returns songCount per playlist. List page shows "N songs" and "Last updated [date]" for each row.</done>
</task>

<task type="auto">
  <name>Task 2: Fix save redirect — dismiss builder before navigation</name>
  <files>src/app/playlists/page.tsx, src/components/playlist-builder.tsx</files>
  <action>
In `src/app/playlists/page.tsx` handleSave function (line ~59):
- Add `setShowBuilder(false)` BEFORE `router.push(\`/playlists/${data.id}\`)`. The builder being mounted shadows the navigation push because the builder renders as a full-screen overlay that replaces the page content via conditional rendering at line ~89. Dismissing it first allows the router.push to take effect on the underlying playlists page component.

In `src/components/playlist-builder.tsx` Save button onClick (line ~255):
- The current code is `onClick={() => onSave(name, items)}` which fires the async onSave without awaiting it. Change to `onClick={async () => { await onSave(name, items); }}` so the save completes (including the API call and redirect) before the handler returns. This prevents race conditions where the component unmounts mid-save.
  </action>
  <verify>
    <automated>cd /Users/hector/Developer/Song\ Tool && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>After saving a playlist in PlaylistBuilder, user is navigated to /playlists/[id] (the new playlist's detail page), not back to /playlists.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Navigate to /playlists — each row shows song count and "Last updated [date]"
3. Create a new playlist via PlaylistBuilder — after save, browser navigates to /playlists/[newId]
</verification>

<success_criteria>
- Playlists list shows song count per playlist
- Date is prefixed with "Last updated"
- Save redirect lands on the new playlist's detail page
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-playlist-builder/04-05-SUMMARY.md`
</output>
