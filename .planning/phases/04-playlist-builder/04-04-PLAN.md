---
phase: 04-playlist-builder
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - src/components/ui/playlist-editor.tsx
  - src/app/api/playlists/[id]/songs/route.ts
  - tests/e2e/playlists.spec.ts
autonomous: false
requirements: [PLAY-03]

must_haves:
  truths:
    - "User can drag a song row to a new position using mouse or touch"
    - "Drag shows a ghost (DragOverlay) of the dragged row while dragging"
    - "On drag end, the list reorders optimistically in the UI"
    - "On drag end, PUT /api/playlists/[id]/songs is called with the full ordered songId array"
    - "After page reload, songs appear in the new dragged order (persisted to DB)"
    - "If the PUT request fails, the UI reverts to the pre-drag order"
    - "Keyboard drag-and-drop works (KeyboardSensor from dnd-kit)"
  artifacts:
    - path: "src/components/ui/playlist-editor.tsx"
      provides: "Complete PlaylistEditor with dnd-kit drag-and-drop, optimistic reorder, persist on drag end, error rollback"
    - path: "src/app/api/playlists/[id]/songs/route.ts"
      provides: "PUT handler accepting ordered songId array, recalculates sequential positions in transaction"

---

<objective>
Finalize the drag-and-drop reorder behavior in PlaylistEditor: optimistic UI update on drag, persist via PUT /api/playlists/[id]/songs, rollback on failure, DragOverlay for visual feedback.

Purpose: This plan delivers PLAY-03 — the drag-and-drop ordering that persists after page reload.
</objective>
