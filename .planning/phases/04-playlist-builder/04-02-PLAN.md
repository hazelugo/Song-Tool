---
phase: 04-playlist-builder
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/playlist-builder.tsx
  - src/app/songs/page.tsx
  - src/app/discovery/page.tsx
  - src/app/playlists/page.tsx
autonomous: true
requirements: [PLAY-01]

must_haves:
  truths:
    - "PlaylistBuilder component renders a full-screen inline view with a drag-sortable song list, name input, and Save button"
    - "Songs can be dragged to reorder within PlaylistBuilder before saving"
    - "Songs can be removed from the builder list before saving"
    - "Songs from catalog can be added to builder from the available songs section"
    - "Clicking Save calls POST /api/playlists with name + ordered items and navigates to /playlists/[id]"
    - "Songs page has 'Save results as Playlist' button that opens PlaylistBuilder with current filtered songs"
    - "Discovery page has 'Save results as Playlist' button that opens PlaylistBuilder with search results"
    - "Playlists list page (GET /api/playlists) shows all playlists with name, song count, last updated"
    - "Playlists list page has 'New Playlist' button that opens PlaylistBuilder"
  artifacts:
    - path: "src/components/playlist-builder.tsx"
      provides: "PlaylistBuilder component — full-screen inline playlist creation with dnd-kit sort"
      exports: ["PlaylistBuilder", "PlaylistItem"]
    - path: "src/app/playlists/page.tsx"
      provides: "Playlists list page — lists all saved playlists with name/count/date; New Playlist button"

---

<objective>
Build the PlaylistBuilder component for creating new playlists and wire the "Save results as Playlist" entry point on Songs and Discovery pages. Also build the Playlists list page.

Purpose: This plan delivers PLAY-01 — users can save a song set as a named playlist from anywhere in the app.
</objective>
