---
phase: 04-playlist-builder
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/playlist-editor.tsx
  - src/components/ui/export-menu.tsx
  - src/components/ui/suggestions-panel.tsx
  - src/app/playlists/[id]/page.tsx
autonomous: true
gap_closure: true
requirements: [PLAY-02, PLAY-04]
must_haves:
  truths:
    - "Playlist detail song rows show YouTube and Spotify link buttons when URLs exist"
    - "Exported CSV and text include Artist and Time Signature columns"
    - "Suggestions are accessible via a prominent 'Suggest songs' button in the header"
  artifacts:
    - path: "src/components/ui/playlist-editor.tsx"
      provides: "YouTube/Spotify link buttons in song rows"
      contains: "youtubeUrl"
    - path: "src/components/ui/export-menu.tsx"
      provides: "Artist and Time Signature in CSV and JSON export"
      contains: "artist"
    - path: "src/app/playlists/[id]/page.tsx"
      provides: "Suggest songs button in header area"
      contains: "Suggest"
  key_links:
    - from: "src/components/ui/playlist-editor.tsx ItemRow"
      to: "Song interface"
      via: "youtubeUrl/spotifyUrl fields"
      pattern: "youtubeUrl|spotifyUrl"
    - from: "src/app/playlists/[id]/page.tsx"
      to: "SuggestionsPanel"
      via: "Button trigger instead of inline collapsible"
      pattern: "Suggest"
---

<objective>
Add YouTube/Spotify buttons to playlist detail rows, include Artist and Time Signature in exports, and make the Suggestions panel more discoverable via a header button.

Purpose: Three enhancements to the playlist detail page — streaming links were never implemented in the editor component, export is missing requested fields, and the suggestions feature has poor discoverability.
Output: Complete playlist detail experience with streaming links, full export data, and prominent suggestions access.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/components/ui/playlist-editor.tsx
@src/components/ui/export-menu.tsx
@src/components/ui/suggestions-panel.tsx
@src/app/playlists/[id]/page.tsx
@src/db/schema.ts
</context>

<interfaces>
<!-- Key types from the DB schema that the executor needs -->

From src/db/schema.ts (songs table columns relevant to this plan):
- artist: text("artist") — nullable
- timeSignature: timeSignatureEnum("time_signature") — not null, default "4/4"
- youtubeUrl: text("youtube_url") — nullable
- spotifyUrl: text("spotify_url") — nullable

The playlist detail page query (playlists/[id]/page.tsx line 26-34) already uses `with: { songs: { with: { song: { with: { tags: true } } } } }` so the full Song object including artist, timeSignature, youtubeUrl, spotifyUrl is already available in the data — it just needs to be passed through to the components.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add YouTube/Spotify buttons to PlaylistEditor and update exports</name>
  <files>src/components/ui/playlist-editor.tsx, src/components/ui/export-menu.tsx, src/app/playlists/[id]/page.tsx</files>
  <action>
**PlaylistEditor — streaming link buttons (src/components/ui/playlist-editor.tsx):**

1. Update the local `Song` interface (line ~31) to add:
   - `youtubeUrl: string | null;`
   - `spotifyUrl: string | null;`
   - `artist: string | null;`

2. In the `ItemRow` component, after the BPM span and before the remove button, add streaming link buttons:
   ```
   {song.youtubeUrl && (
     <a href={song.youtubeUrl} target="_blank" rel="noopener noreferrer"
        className="opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity text-muted-foreground hover:text-red-500 shrink-0">
       <svg className="h-4 w-4" ...YouTube icon... />
     </a>
   )}
   {song.spotifyUrl && (
     <a href={song.spotifyUrl} target="_blank" rel="noopener noreferrer"
        className="opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity text-muted-foreground hover:text-green-500 shrink-0">
       <svg className="h-4 w-4" ...Spotify icon... />
     </a>
   )}
   ```
   Use inline SVG icons for YouTube (play triangle in rounded rectangle) and Spotify (three curved lines). Keep them small (h-4 w-4). Place them in a flex container between the song info and the remove button. Show on hover only (matching the remove button pattern) on desktop, always visible on mobile.

3. Also show artist name in the song row if present — add below the song name as a small muted text: `{song.artist && <span className="text-xs text-muted-foreground">{song.artist}</span>}`

**ExportMenu — add Artist and Time Signature (src/components/ui/export-menu.tsx):**

1. Update the `ExportSong` interface to add:
   - `artist: string | null;`
   - `timeSignature: string | null;`

2. In `handleCsv`:
   - Change headers to: `["Name", "Artist", "BPM", "Key", "Key Signature", "Time Signature", "Tags"]`
   - Add `s.artist ?? ""` and `s.timeSignature ?? ""` to each row array in the correct positions (after Name and after Key Signature respectively).

3. In `handleJson`:
   - Add `artist: s.artist` and `timeSignature: s.timeSignature` to the song objects.

4. In `handlePrint` and `handlePrintWithLyrics`:
   - Add Artist and Time Signature columns/metadata to the printed output.

**Playlist detail page — pass new fields through (src/app/playlists/[id]/page.tsx):**

1. In the ExportMenu songs prop mapping (line ~70), add:
   - `artist: ps.song.artist,`
   - `timeSignature: ps.song.timeSignature,`
   The data is already available from the Drizzle query — it just needs to be passed through.
  </action>
  <verify>
    <automated>cd /Users/hector/Developer/Song\ Tool && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Playlist detail rows show YouTube/Spotify icon links when URLs exist. CSV/JSON/print exports include Artist and Time Signature columns.</done>
</task>

<task type="auto">
  <name>Task 2: Surface suggestions as a header button instead of buried collapsible</name>
  <files>src/components/ui/suggestions-panel.tsx, src/app/playlists/[id]/page.tsx</files>
  <action>
**Refactor SuggestionsPanel (src/components/ui/suggestions-panel.tsx):**

1. Change the component to accept an `open` boolean prop and an `onOpenChange` callback instead of managing its own open state internally. Keep the internal `suggestions`, `loading`, and `addingIds` state.

2. Update the interface:
   ```
   interface SuggestionsPanelProps {
     playlistId: string;
     existingSongIds: string[];
     open: boolean;
     onOpenChange: (open: boolean) => void;
   }
   ```

3. Remove the collapsible header button entirely. The component should only render its content (the suggestions list) when `open` is true. Remove the `<button onClick={handleToggle}>` wrapper and the outer border/card container — the parent will handle placement.

4. Keep the `load` function. Trigger it via `useEffect` when `open` changes to `true` and `suggestions.length === 0`.

5. Wrap the suggestions content in a bordered card container: `<div className="border rounded-sm bg-card overflow-hidden">` with a small header showing "Suggested Songs" and a close button (X icon) that calls `onOpenChange(false)`.

**Update playlist detail page (src/app/playlists/[id]/page.tsx):**

1. The page is a server component. The SuggestionsPanel needs client-side state for open/close. Create a small client wrapper component (can be inline in the same file or a separate tiny component) that:
   - Has `const [showSuggestions, setShowSuggestions] = useState(false);`
   - Renders a "Suggest songs" Button (with Sparkles icon from lucide-react) in the header action buttons area (next to Export and Add Songs)
   - The button: `<Button size="sm" variant="outline" onClick={() => setShowSuggestions(true)} className="h-10 md:h-7 text-xs rounded-sm"><Sparkles className="h-3.5 w-3.5 mr-1.5" />Suggest songs</Button>`
   - When showSuggestions is true, renders SuggestionsPanel below the PlaylistEditor (in the same position it was before, but now controlled by the button).

2. Since the page is a server component and we need client interactivity, wrap the header actions + suggestions panel in a client component. Name it `PlaylistActions` or similar. It receives `playlistId`, `existingSongIds`, `playlistName`, `songs` (for export), and the `children` slot for the Live link. It renders the action buttons (Suggest, Export, Add Songs) and conditionally the SuggestionsPanel.

3. Remove the standalone `<SuggestionsPanel>` from the bottom of the page — it's now inside the client wrapper.
  </action>
  <verify>
    <automated>cd /Users/hector/Developer/Song\ Tool && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>A "Suggest songs" button with Sparkles icon appears in the playlist detail header. Clicking it opens the suggestions panel inline below the song list. The old buried collapsible is removed.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Playlist detail rows show YouTube (red) and Spotify (green) icon links on hover when URLs exist
3. Export CSV includes columns: Name, Artist, BPM, Key, Key Signature, Time Signature, Tags
4. "Suggest songs" button visible in header; clicking it reveals the suggestions panel
5. Suggestions panel can be closed and reopened
</verification>

<success_criteria>
- YouTube/Spotify link buttons render conditionally in playlist detail song rows
- CSV and JSON exports include Artist and Time Signature fields
- Suggestions panel is triggered by a prominent header button, not a hidden collapsible
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-playlist-builder/04-07-SUMMARY.md`
</output>
