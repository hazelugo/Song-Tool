---
phase: 04-playlist-builder
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/playlist-builder.tsx
autonomous: true
gap_closure: true
requirements: [PLAY-01, PLAY-03]
must_haves:
  truths:
    - "Adding a song in PlaylistBuilder removes it from the available songs list"
    - "Drag-and-drop in PlaylistBuilder is smooth with no visual jank"
  artifacts:
    - path: "src/components/playlist-builder.tsx"
      provides: "Filtered available songs excluding already-added; DragOverlay with activation constraint"
      contains: "DragOverlay"
  key_links:
    - from: "src/components/playlist-builder.tsx filteredSongs"
      to: "items state"
      via: "Set exclusion filter"
      pattern: "itemIds.*has"
    - from: "src/components/playlist-builder.tsx DndContext"
      to: "DragOverlay"
      via: "activeId state tracking"
      pattern: "DragOverlay"
---

<objective>
Fix two PlaylistBuilder UX issues: songs staying in the available pool after being added, and janky drag-and-drop.

Purpose: Both issues degrade the PlaylistBuilder experience — added songs appearing as still available is confusing, and janky drag makes reordering frustrating.
Output: Clean PlaylistBuilder with proper song filtering and smooth drag behavior matching PlaylistEditor quality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/components/playlist-builder.tsx
@src/components/ui/playlist-editor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter added songs from available pool and add DragOverlay</name>
  <files>src/components/playlist-builder.tsx</files>
  <action>
**Fix 1 — Filter added songs from available pool (line ~159):**
- Before the `filteredSongs` derivation, build a Set of song IDs currently in the playlist: `const itemSongIds = new Set(items.map(item => item.song.id));`
- In the `filteredSongs` filter chain, add an additional condition: `&& !itemSongIds.has(song.id)` so songs already added to the playlist are excluded from the library panel.
- Use `useMemo` for both the Set and the filtered list to avoid recomputing on every render. Dependencies: `items` for the Set, `availableSongs`, `searchQuery`, `itemSongIds` for filteredSongs.

**Fix 2 — Add DragOverlay for smooth drag (matching PlaylistEditor pattern):**
- Import `DragOverlay`, `DragStartEvent` from `@dnd-kit/core` (add to existing import).
- Add `activeId` state: `const [activeId, setActiveId] = useState<string | null>(null);`
- Add `onDragStart` handler: `const handleDragStart = (event: DragStartEvent) => { setActiveId(event.active.id as string); };`
- Update `handleDragEnd` to call `setActiveId(null)` at the start.
- Add `activationConstraint: { distance: 8 }` to the PointerSensor config (prevents accidental drags on clicks).
- Add `onDragStart={handleDragStart}` and `onDragCancel={() => setActiveId(null)}` to the DndContext.
- After `</SortableContext>`, add a `<DragOverlay dropAnimation={null}>` that renders a clone of the active item when `activeId` is set. Find the active item: `const activeItem = activeId ? items.find(i => i.id === activeId) : null;`
- In the DragOverlay, render a simplified song row (name + BPM + key) styled similarly to SortablePlaylistItem but with a shadow and no opacity change.
- In the existing `SortablePlaylistItem`, add conditional opacity: when `isDragging` is true, show `opacity-30` (placeholder effect) instead of `opacity-50`. This is because DragOverlay handles the visual clone — the original just needs to be a subtle placeholder.
  </action>
  <verify>
    <automated>cd /Users/hector/Developer/Song\ Tool && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Added songs disappear from the library panel. Drag-and-drop uses DragOverlay with activation constraint — smooth, no jank, matching PlaylistEditor quality.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. In PlaylistBuilder, add a song from the library — it disappears from the library list immediately
3. Remove a song from the playlist — it reappears in the library list
4. Drag a song in the playlist — smooth overlay follows cursor, placeholder stays in place with reduced opacity, no visual jank
</verification>

<success_criteria>
- Added songs are excluded from the available songs panel
- Removing a song from the playlist makes it reappear in the library
- Drag is smooth with DragOverlay (no DOM node jumping)
- Activation constraint prevents accidental drags
</success_criteria>

<output>
After completion, create `.planning/phases/04-playlist-builder/04-06-SUMMARY.md`
</output>
