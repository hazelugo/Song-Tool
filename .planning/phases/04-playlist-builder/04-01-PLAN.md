---
phase: 04-playlist-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/playlists/route.ts
  - src/app/api/playlists/[id]/route.ts
  - src/app/api/playlists/[id]/songs/route.ts
  - src/app/api/playlists/[id]/songs/[songId]/route.ts
  - src/app/api/playlists/[id]/suggestions/route.ts
  - src/lib/ranking.ts
  - src/lib/camelot.ts
  - tests/e2e/playlists.spec.ts
  - package.json
autonomous: true
requirements: [PLAY-01, PLAY-02, PLAY-05, PLAY-06]

must_haves:
  truths:
    - "GET /api/playlists returns playlist list ordered by updatedAt desc"
    - "POST /api/playlists creates a playlist with optional initial songs"
    - "GET /api/playlists/[id] returns playlist with songs + song tags, ordered by position"
    - "PATCH /api/playlists/[id] renames the playlist"
    - "DELETE /api/playlists/[id] soft-deletes (sets deletedAt), returns 204"
    - "POST /api/playlists/[id]/songs adds songs to an existing playlist (appends to end)"
    - "PUT /api/playlists/[id]/songs reorders songs (full ordered songId array, recalculates positions)"
    - "DELETE /api/playlists/[id]/songs/[songId] removes a song from a playlist (join table row only)"
    - "GET /api/playlists/[id]/suggestions returns top 10 Camelot-compatible songs not in playlist"
    - "src/lib/ranking.ts exports generateRank(prev, next) and rebalanceRanks(items)"
    - "src/lib/camelot.ts exports getCamelotPosition, getKeyCompatibility, formatCamelot"
    - "e2e stubs exist for PLAY-01 through PLAY-06"
  artifacts:
    - path: "src/lib/ranking.ts"
      provides: "Fractional indexing helpers"
      exports: ["generateRank", "rebalanceRanks"]
    - path: "src/lib/camelot.ts"
      provides: "Camelot Wheel key compatibility scoring"
      exports: ["getCamelotPosition", "getKeyCompatibility", "formatCamelot", "CamelotPosition"]
    - path: "src/app/api/playlists/route.ts"
      provides: "GET (list) + POST (create) for playlists"
    - path: "src/app/api/playlists/[id]/route.ts"
      provides: "GET (detail with songs) + PATCH (rename) + DELETE (soft)"
    - path: "src/app/api/playlists/[id]/songs/route.ts"
      provides: "POST (add songs) + PUT (reorder songs)"
    - path: "src/app/api/playlists/[id]/songs/[songId]/route.ts"
      provides: "DELETE (remove song from playlist)"
    - path: "src/app/api/playlists/[id]/suggestions/route.ts"
      provides: "GET (Camelot-based suggestions)"

---

<objective>
Build the complete playlist data layer: all API routes for playlist CRUD and song management, fractional indexing library, and Camelot harmonic compatibility library.

Purpose: This is the server foundation for the entire playlist feature. All UI in Plans 02–04 depends on these routes and libraries.
</objective>
