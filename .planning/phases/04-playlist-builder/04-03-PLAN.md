---
phase: 04-playlist-builder
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/app/playlists/[id]/page.tsx
  - src/components/ui/playlist-editor.tsx
  - src/components/ui/add-songs-dialog.tsx
  - src/components/ui/suggestions-panel.tsx
  - src/components/ui/export-menu.tsx
  - src/components/ui/playlist-name-editor.tsx
autonomous: false
requirements: [PLAY-02, PLAY-04, PLAY-05, PLAY-06]

must_haves:
  truths:
    - "Playlist detail page shows all songs in saved order with Name, BPM, Key, streaming links (YouTube/Spotify)"
    - "Songs in playlist have clickable YouTube and Spotify links that open in a new tab"
    - "User can remove a song from the playlist (DELETE /api/playlists/[id]/songs/[songId])"
    - "User can rename the playlist inline without leaving the page (PATCH /api/playlists/[id])"
    - "User can add songs from the catalog to the playlist via AddSongsDialog (POST /api/playlists/[id]/songs)"
    - "User can see Camelot-compatible song suggestions in the SuggestionsPanel and add them in one click"
    - "User can export the playlist as CSV or text via ExportMenu (client-side blob download)"
    - "User can delete the playlist from the detail page (DELETE /api/playlists/[id]) and is redirected to /playlists"
  artifacts:
    - path: "src/app/playlists/[id]/page.tsx"
      provides: "Playlist detail page — server component fetching playlist + songs; renders PlaylistEditor, AddSongsDialog, SuggestionsPanel, ExportMenu, PlaylistNameEditor"
    - path: "src/components/ui/playlist-editor.tsx"
      provides: "PlaylistEditor — song rows with drag handles, streaming links, remove button; dnd-kit sortable"
      exports: ["PlaylistEditor"]
    - path: "src/components/ui/add-songs-dialog.tsx"
      provides: "AddSongsDialog — modal search + select songs from catalog; POST /api/playlists/[id]/songs on save"
      exports: ["AddSongsDialog"]
    - path: "src/components/ui/suggestions-panel.tsx"
      provides: "SuggestionsPanel — collapsible; fetches GET /api/playlists/[id]/suggestions; one-click add"
      exports: ["SuggestionsPanel"]
    - path: "src/components/ui/export-menu.tsx"
      provides: "ExportMenu — dropdown for CSV and text export (client-side blob download)"
      exports: ["ExportMenu"]
    - path: "src/components/ui/playlist-name-editor.tsx"
      provides: "PlaylistNameEditor — click-to-edit inline; PATCH /api/playlists/[id] on save"
      exports: ["PlaylistNameEditor"]

---

<objective>
Build the playlist detail page UI: PlaylistEditor for song rows with streaming links and remove action, AddSongsDialog, SuggestionsPanel, ExportMenu, and PlaylistNameEditor — all wired to the API layer from Plan 01.

Purpose: This plan delivers PLAY-02 (view playlist), PLAY-04 (streaming links), PLAY-05 (remove song), and the bonus features (rename, add songs, suggestions, export).
</objective>
